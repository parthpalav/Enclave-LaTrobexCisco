# Frontend build-time configuration.
# Leave VITE_API_BASE empty in dev to use the Vite proxy (see vite.config.ts).
# In production point it at the backend origin, e.g. https://api.example.com
VITE_API_BASE=

# Used only by the Vite dev proxy target.
VITE_PROXY_TARGET=http://localhost:8000
